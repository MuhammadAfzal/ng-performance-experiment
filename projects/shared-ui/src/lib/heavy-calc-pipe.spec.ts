import { HeavyCalcPipe } from './heavy-calc-pipe';

describe('HeavyCalcPipe', () => {
  it('create an instance', () => {
    const pipe = new HeavyCalcPipe();
    expect(pipe).toBeTruthy();
  });
});
